# The count() method in Python is used to count the number of occurrences of a substring in a string. It takes the substring as an argument and returns the number of times it appears in the string.
str = "hello $my na$me is $aman$"
count = str.count("$")
print("Number of occurrences of '$':", count)