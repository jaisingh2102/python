#tuple:- a built-in data structure that stores multiple elements of different data types in a single variable.
#it is immutable, ordered,and allows duplicate elements.tuples are defined using parentheses () and elements are separated by commas.
tup = (1,2,3,2,1,5)
print(type(tup))
print(tup)
tup[1:4]
print(tup[1:4])